name: Export V13 Canonical File

on:
  push:
    paths:
      - '.github/workflows/export-v13.yml'

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: owner-website-v13
          path: index.html
          if-no-files-found: error
